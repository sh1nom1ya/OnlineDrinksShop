@model IEnumerable<drunkShop.Models.ApplicationType>
<link rel="stylesheet" href="~/css/showDB.css" asp-append-version="true" />

<div class="wrapper-db">
    <div class="cont">
        <div class="zagolovok">
            <h2>Список типов</h2>
        </div>
        <div class="forBtnCreate">
            <a asp-action="Create" class="btnAddDB">+ Создать тип</a>
        </div>

        @if (Model.Count() > 0)
        {
            <table class="tableDB">
                <thead>
                    <tr>
                        <th>Название</th>
                        <th></th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var obj in Model)
                    {
                        <tr>
                            <td>@obj.Naming</td>
                            <td>
                                <div class="btn-group" role="group">
                                    <a asp-controller="ApplicationType" asp-route-Id="@obj.Id" asp-action="Edit" class="btnEdit">
                                        <img src="~/Images/edit.png" alt="">
                                    </a>
                                    <a asp-controller="ApplicationType" asp-route-Id="@obj.Id" asp-action="Delete" class="btnDelete">
                                        <img src="~/Images/delete.png" alt="">
                                    </a>
                                </div>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        }
        else
        {
            <p>
                Пока не создано ни одного типа
            </p>
        }
    </div>
</div>